# Copy/Paste Prompt for Google Antigravity

You are working in an empty workspace that must become the `retriva-eval` repository.

Use the SDD pack in this workspace as the source of truth.

Create the MVP repository for continuous evaluation of Retriva, implementing only the first suite: `ragas_sample_markdown`.

Important constraints:

- Retriva uses Qdrant as vector DB.
- For MVP, the Qdrant evaluation collection is created manually by the user.
- The eval framework must verify the configured collection exists.
- The eval framework must never delete the manually configured collection.
- Design the Qdrant adapter so future per-suite collection creation/deletion can be added later.
- Include dry-run mode so tests can pass without LLM credentials.

Expected commands after implementation:

```bash
pip install -e .[dev]
pytest
retriva-eval list-suites
retriva-eval run-cycle pipelines/smoke.yaml --dry-run
```

Deliver:

- production-quality Python package
- first suite implementation
- README
- configs
- tests
- reports generated by dry-run smoke execution
